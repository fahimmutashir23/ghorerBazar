import { url } from "../../connection";


const product = `${url}/Upload/product/images/`;
const profile = `${url}/Upload/profile/images/`;
const banner = `${url}/Upload/banner/images/`;

export const imgUrl = {product, profile, banner}